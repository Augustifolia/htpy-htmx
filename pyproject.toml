[project]
name = "htpy-htmx"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "htpy>=26.5.1",
]

[dependency-groups]
dev = [
    "black>=26.5.1",
    "mypy>=2.3.0",
    "pytest>=9.1.1",
]
